{% extends 'layouts/base.html.twig' %}

{% block title %} Edit {{ pin.title }} {% endblock %}

{% block body %}

    <div class="row">
        <div class="col-md-6 mx-auto">

            <h1> Edit </h1>

            {{ form_start(form) }}
            {{ form_widget(form) }}

            <input type="submit" class="btn btn-primary btn-block" value="Update" formnovalidate>
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}
